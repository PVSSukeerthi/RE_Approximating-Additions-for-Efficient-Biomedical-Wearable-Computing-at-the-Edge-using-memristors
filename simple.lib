library(simple) {
  cell(AND2) {
    area: 1;
    pin(A) { direction: input; }
    pin(B) { direction: input; }
    pin(Y) {
      direction: output;
      function: "A & B";
    }
  }
  cell(OR2) {
    area: 1;
    pin(A) { direction: input; }
    pin(B) { direction: input; }
    pin(Y) {
      direction: output;
      function: "A | B";
    }
  }
  cell(NOT1) {
    area: 1;
    pin(A) { direction: input; }
    pin(Y) {
      direction: output;
      function: "!A";
    }
  }
  cell(XOR2) {
    area: 1;
    pin(A) { direction: input; }
    pin(B) { direction: input; }
    pin(Y) {
      direction: output;
      function: "A ^ B";
    }
  }
    cell(BUF) {
    area: 1;
    pin(A) { direction: input; }
    pin(Y) {
      direction: output;
      function: "A";
    }
  }

}
